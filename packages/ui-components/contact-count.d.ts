import React from 'react';
export interface ContactCountProps {
    count?: string | number;
    isRight?: boolean;
    className?: string;
}
export declare const ContactCount: React.SFC<ContactCountProps>;
export default ContactCount;
